import numpy as np
from PIL import Image

standard_images = ['standard1.jpg','standard2.jpg','standard3.jpg']

new_phone_test_images = ['test1.jpg','test2.jpg','test3.jpg']

#can write if necessary (if the algorithm doesn't work well without this). 
#This file should be able to figure out a transformation function
#that can convert a new phone's images into the same image as if it was taken by
#the phone that we are using as the standard.
